# ai-app-local-data-rag
This example demonstrates how to build a "defensible AI application" by integrating proprietary data and custom retrieval logic, rather than just being a simple wrapper around an LLM API. It uses a local product database and a simulated LLM to show how an application can add unique value by providing curated context to the AI model. The core value 
